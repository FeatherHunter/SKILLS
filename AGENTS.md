## Agent skills

### Issue tracker

Local markdown at `.scratch/<feature>/issues/<NN>-<slug>.md` (对齐 SKILL开发总纲V1.0 约定). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: root `CONTEXT.md` + root `docs/adr/`(lazy create). See `docs/agents/domain.md`.

> Triage labels: skipped — `triage` skill 未安装,Section B 不运行。如未来安装 triage skill,重新跑 setup-matt-pocock-skills。