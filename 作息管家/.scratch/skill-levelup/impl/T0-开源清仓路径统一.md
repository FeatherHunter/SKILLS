## Question

执行 R2 处置清单：开源清仓 + 路径统一。实施 map T0（先决，无依赖）。

## 文件

- `scripts/schedule_db.py`（get_db_base_dir 统一链：env → D:/.db(win) → ~/.local/share/schedule-guardian/db(linux)）
- `scripts/schedule_html_render.py:377` / `scripts/help_render.py:50`（import 复用统一解析器）
- `scripts/validators.py`（白名单路径）+ `scripts/migrate_categories.py`（引用 DB_PATH）
- 删除：`scripts/direct_add.py` / `batch_add_morning.py` / `plan*.json`（git rm）
- `.gitignore` 修正 + `LICENSE`（MIT 王辰浩）+ 文档绝对路径 8 处

## 交付

处置清单 16 项全落地（清单见规格 `.scratch/skill-levelup/issues/R2-开源准备处置.md`）；pytest 全过。

## 门禁

门禁 A（垂直 5 层对账）+ 偏离记录三问 + Tested-By。
