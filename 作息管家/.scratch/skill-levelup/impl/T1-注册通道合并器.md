## Question

实现渐进式注册通道 + 场景合并器（对抗式审查矛盾 5 修正：不动现有 49 命令分发）。

## 文件

- `scripts/schedule_cli.py`（**末尾追加**动态加载段：发现域模块 `COMMANDS` 注册表，自动 dispatch；现有 if/elif 不动）
- `scripts/update_scenarios.py`（合并器：读 `scenarios/*.yaml` 片段 → 校验字段契约 → 拼接待办 T8 写回 references/scenarios.yaml，幂等）

## 交付

域模块自带 COMMANDS → CLI 自动发现；合并器可运行（T8 用）；fixture 隔离约定成文（SKILLS_DB_PATH=临时目录）。

## 依赖

← T0（路径统一先落）
