## Question

批量导入 batch-add（真新增，自包含）。

## 文件

- `scripts/batch_scenarios.py`（自带 COMMANDS 注册表：batch-add <date> --json @records.json，校验复用 add 链路）
- `scenarios/batch.yaml`（场景片段）+ `tests/`（fixture DB）

## 交付

batch-add 命令模块，自注册进 CLI；幂等不提供（记录无唯一键，文档注明）；一 commit。

## 依赖

← T1（注册通道）；不碰 schedule_cli.py / scenarios.yaml
