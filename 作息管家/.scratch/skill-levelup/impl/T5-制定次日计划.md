## Question

制定次日计划（真新增，强化 #17 商量计划）。

## 文件

- `scripts/plan_scenarios.py`（COMMANDS）+ `templates/plan_result.html`（时间轴 + 分类色带 + 历史贴合提示）
- `scenarios/plan.yaml` + `tests/`

## 交付

计划结果 HTML 呈现强化；多轮交互（生成→调整→再生成）走既有商量计划流程；一 commit。

## 依赖

← T1；不碰 schedule_cli.py / scenarios.yaml
