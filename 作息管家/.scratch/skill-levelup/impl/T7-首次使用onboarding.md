## Question

首次使用 + onboarding（真新增；依赖仅 T0+T1，零基础优先——对抗式审查矛盾 4 修正）。

## 文件

- `scripts/setup_scenarios.py`（COMMANDS：check/init/status 编排）+ `templates/开始使用/first_use_wizard.html`（对标居家管家）
- `scenarios/setup.yaml`（「首次使用」场景定义）

## 交付

复制即装（R3 三段式）+ 6 步初始化 + 飞书强引导（配合飞书效果最好，拒绝才跳过）+ 初始化报告 HTML；一 commit。

## 依赖

← T1（不需等 T2/T3/T4/T5/T6）
