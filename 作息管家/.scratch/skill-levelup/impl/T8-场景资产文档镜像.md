## Question

场景资产合并 + 文档 + 镜像（收尾，依赖全部）。

## 文件

- `references/scenarios.yaml`（合并器全量拼接：73→81，含替换关系）
- `README.md`（新建，30 秒开始）+ `SKILL.md`（首次使用章节 + 场景注册替换 + 模板分合规则）
- help 镜像（跑 `help_render.py` 同步 `作息管家.html`，ADR-0001）

## 交付

81 场景落 yaml；替换关系落文（记录→#0 系列 / 制定计划→#17 / 批量导入→#0-json / 复盘区间=start-end 更名）；场景片段全部合并校验。

## 依赖

← 全部 T0-T7
