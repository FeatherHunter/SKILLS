## Question

产出 L 级重构后的**完整场景清单**：73 → 74+，含「首次使用」场景（Q9）+ G1 定标的新功能场景；登记 `scenarios.yaml` 的改动点清单（经合并器写入）。

## 背景

- 现状：`references/scenarios.yaml` 73 场景（唯一事实源）
- Q9 拍板：新增「首次使用」唤醒词 + 初始化工作流 → 至少 74
- G1 拍板后可能再增新功能场景

## 阻塞

- **blocked by**：G1 领域第一功能定标、R3 onboarding 设计（场景依赖功能与 onboarding 定义）

## 交付

场景清单（编号 + 名称 + 所属域 record/plan/onboarding + 对应模板/命令 + 交互形态：一步直达/多轮过程），作为实施阶段场景资产改动输入。
