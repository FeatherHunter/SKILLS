## Question

把 Q5/Q6/Q7/Q14 决策落成**可执行的开源清仓处置清单**：哪些文件改什么、新增什么、删除什么，作为实施阶段的直接输入。

## 背景事实（已盘点，勿重查）

- `.gitignore` 漂移：`.db/` 派生产物 + `__pycache__/` 已 commit，规则与实际不符（当前仍跟踪 `作息管家/.db/category_whitelist.yaml`）
- 直连脚本：`scripts/batch_add.py`（唯一批量导入入口 → 收敛为 CLI `batch-add` 子命令）、`scripts/direct_add.py`（与 batch_add 重复 85% + 硬编码 2026-05-30 一次性数据 → 删）、根 `batch_add_morning.py`（8 月晨练计划一次性任务已完成 → 删）
- 路径 3 套不一致：`schedule_db.py:66-76`（平台感知：win→`D:/.db`、WSL→`/mnt/d/.db`、其他→抛错）、`schedule_html_render.py:377`（硬编码 `D:/.db`）、`help_render.py:50` + `validators.py:22`（`SKILL_DIR/.db` 相对路径）
- 文档含绝对路径 5 处：SKILL.md L778-779、CHANGELOG.md L828/L1158、docs/agents/*
- `作息管家/` 无独立 LICENSE（根仓库为 MIT）

## 已定决策（人类拍板）

- Q5 开源清仓全部处理；Q6 路径统一链 `SKILLS_DB_PATH env → D:/.db(win) → ~/.local/share/schedule-guardian/db(linux)`；Q7 独立 MIT 子 LICENSE；Q14 收敛为「底层 schedule_db.py + 中间层 schedule_cli.py」两层；Q10 历史不清理、README 不提及

## 交付

处置清单（markdown）：每项文件 + 处置方式（删/改/收敛/新增）+ 具体改动点；LICENSE 文件内容草案；`batch-add` 子命令设计（参数、校验、分类逻辑复用）。决议评论挂本票。
