---
name: 作息管家
description: 根据用户每日语录生成0-24点作息时间表，支持同步、摘要、查询、计划管理。触发词：作息、同步作息、总结作息、查计划等。
metadata: { "openclaw": { "emoji": "🌙", "requires": { "python": ">=3.7" } } }
---

> **版本**: 2026-05-22 重构版

---

## 功能速查

| # | 功能 | 触发词 | CLI命令 |
|---|------|--------|---------|
| 1 | 同步作息 | "同步作息" "更新作息表" | `prepare-messages` → AI分析 → `add-record` |
| 2 | 生成摘要 | "总结作息" "生成摘要" | `generate-summary <日期>` |
| 3 | 查询作息 | "作息记录" "今天干了什么" | `list`/`detail`/`summary`/`timeline`/`report`/`range` |
| 4 | 查询计划 | "查计划" "今天计划" | `query-plans <日期>` |
| 5 | 设置计划 | "新建计划" "更新计划" | `upsert-plan <日期> <内容>` |

---

## 核心规则（违反即无效）

1. **禁止直连数据库** — 所有操作必须通过 CLI 接口
2. **禁止 DELETE** — 只允许 INSERT / UPDATE
3. **source_contents 必须是消息原文** — 一字不差，不得总结或臆造
4. **时间必须连续** — 记录覆盖 00:00-23:59，每条 time_start = 上一条 time_end

> 详细规范见: `references/操作规范.md`

---

## 📦 安装与配置

### 依赖

- Python >= 3.7
- 无第三方依赖（仅用标准库 sqlite3、argparse、datetime）

### 配置项

| 环境变量 | 说明 | 默认值 |
|---------|------|--------|
| `SKILLS_DB_PATH` | 数据库文件所在目录 | 技能目录下 `.db/` |

DB 查找顺序：`SKILLS_DB_PATH` 环境变量 → 技能目录 → 父目录 `.db/` 文件夹 → 自动创建 `.db/` 目录。

### 一键安装 prompt

将以下内容发送给 AI 即可安装本技能：

```
请帮我安装作息管家技能：
1. 检查 Python 环境
2. 引导我配置环境变量
3. 显示当前环境变量配置
4. 告诉我如何更改数据目录
```

---

## 功能详细说明

### 1. 同步作息

将语录消息分析为细粒度作息记录。

**执行流程**：
1. 调用 `prepare-messages` 获取消息
2. AI 用滑动窗口（前5后5）判断活动边界
3. 逐条调用 `add_record_full()` 写入（9字段全填）
4. 空白时段填充 `category=未知`
5. 保证首尾相接

**粒度原则**：按活动切换点细分，宁可多条不要合并。

> 详细流程见: `references/同步流程.md`

---

### 2. 生成摘要

按分类汇总当日作息时间。

**前提条件**：该日期的 `schedule_records` 必须已覆盖 24 小时。

**输出格式**：
```
📊 2026-05-22 作息摘要
  😴 睡眠: 7h0m
  💼 工作: 4h0m
  🚴 通勤: 0h36m
  总计: 24h ✓
```

---

### 3. 查询作息

| 命令 | 说明 |
|------|------|
| `list [日期]` | 查看指定日期作息 |
| `detail [日期]` | 详细展示（含AI推理） |
| `summary [日期]` | 查看每日摘要 |
| `timeline [日期]` | 时间轴展示 |
| `report [日期]` | 完整报告 |
| `range <开始> <结束>` | 日期范围统计 |

> 详细命令见: `references/CLI命令.md`

---

### 4. 查询计划

```bash
python3 scripts/schedule_cli.py query-plans <日期1,日期2,...>
# 示例：python3 scripts/schedule_cli.py query-plans 2026-05-22
```

---

### 5. 设置计划

```bash
# JSON方式
python3 scripts/schedule_cli.py upsert-plan <日期> --json '{"hour_0": "睡觉", "hour_8": "30min通勤+30min工作"}'
```

- 日期不存在 → 插入
- 日期已存在 → 更新（保留未填的小时）
- 不提供删除接口

---

## 推荐 Cron 任务

| 任务 | 调度 | 说明 |
|------|------|------|
| 定时同步 | `5 2 * * *`（每天凌晨2:05） | 静默同步，不推送 |
| 每日报告 | `30 7 * * *` | 验证满24h → 生成摘要 → 推送报告 |

> 详细设计见: `references/Cron任务.md`

---

## references/ 目录

| 文件 | 内容 |
|------|------|
| `操作规范.md` | 禁止直连DB、禁止DELETE、全字段校验 |
| `同步流程.md` | 滑动窗口规则、逐条处理、原文写入 |
| `数据库结构.md` | 三表结构、字段说明 |
| `接口清单.md` | 所有接口参数和调用方式 |
| `分类清单.md` | 建议分类列表（AI自由判定） |
| `CLI命令.md` | 所有CLI命令用法 |
| `Cron任务.md` | 定时任务详细设计 |
