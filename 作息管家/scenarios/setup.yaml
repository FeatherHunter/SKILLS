# 首次使用域场景片段(T7 · 2026-08-09)
#
# 位置: 作息管家/scenarios/setup.yaml
# 合并通道: scripts/update_scenarios.py 合并器(实施 T1 建成)读本目录 *.yaml 片段 →
#           校验字段契约(§07 §2.2) → 按 scenario_id 合并写回 references/scenarios.yaml。
#           本片段写回由 T8(场景资产+文档+镜像)执行,此处只保证片段合法可合并。
# 本文件与 references/scenarios.yaml 同构: 顶层为场景列表,每项 7 字段契约。
#
# 设计依据:
# - 规格 R3-onboarding设计 + 首次使用工作流(6 步: 环境检测→路径确认→建库→状态确认→
#   初始化报告→完成)
# - 跨技能「首次使用」流程对齐(根 AGENTS.md 2026-08-07): 对标居家管家 SM8 6 步 +
#   备忘录 9 步,报告数据契约 {items/todos/verify} 同备忘录 init-report
# - 飞书强引导(2026-08-09 人类修正 · 原「可选」升级): 默认引导安装 + 强烈建议配置,
#   说「配合飞书效果最好」;只有用户明确拒绝才跳过,报告页醒目标注「飞书同步不可用」
# - status 判定(07 §4/§6 五者对应): 唤醒词路由(SKILL.md 首次使用章节 · T8 替换)+
#   场景资产 ✓ + prompt ✓ + 底层工作流(scripts/setup_scenarios.py · T7 已建)+ HELP 映射
#   → status 取 ''(可用);T8 落 SKILL.md 路由 + 合并写回后,HELP 呈现该场景卡
- wake_word: 首次使用
  scenario_id: first_use
  scenario_title: 首次使用(初始化工作流)
  dimensions: {}
  prompt: 请帮我初始化作息管家,我是第一次使用(唤醒词:首次使用)
  status: ''
  result: 6 步向导(环境检测→路径确认→建库→状态确认→初始化报告→完成),幂等可重试,已有库不重置;飞书强引导(配合飞书效果最好,拒绝才跳过);初始化报告 HTML + 复制数据/复制日志
