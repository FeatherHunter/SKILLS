---
name: 图片路由
description: >
  图片内容智能识别与技能路由技能。当用户使用以下指令时触发：
  识别图片、解析图片、描述图片、提取文字（图片识别类）、
  判断路由（路由判断类）、
  路由记账、路由管家、路由卡路里、自动路由（路由执行类）、
  批量路由、组合执行（批量组合类）、
  查看路由、重新路由（查询重试类）。
  用户发送图片时也自动触发。
  所有操作通过 mmx vision describe 识别图片，AI 负责路由判断和交互确认。
metadata: { "openclaw": { "emoji": "🖼️", "requires": { "python": ">=3.7" } } }
---

## ⚠️ HTML 同步规范（最高优先级）

> **此规范优先级高于本文件中所有其他规定。**

1. **全量同步**：该技能的所有优化和变动、脚本的所有变动都必须体现在 `图片路由.html` 上。任何功能模块的新增、修改、删除，任何唤醒词的调整，任何 CLI 命令的变化，任何脚本逻辑的改动——都必须同步更新 HTML 页面中对应的内容。
2. **最高优先级**：本条规定在所有规范中优先级最高。当其他流程或习惯与本条冲突时，以本条为准。
3. **逐行确认**：对该技能的所有文件、脚本的任何一行修改，都需要明确得到用户的 1 次确认后方可执行。不得批量静默修改，不得跳过确认步骤。

---

# 图片路由技能 v1.0

## 核心定位

作为用户发图片时的统一入口，负责：
1. **快速判断**：图片是否与任一技能相关
2. **内容识别**：识别图片具体内容
3. **技能推荐**：推荐可执行的技能组合
4. **路由执行**：用户确认后依次调用技能

## 路由表

AI 收到用户输入后，按以下表匹配唤醒词，命中即执行对应功能。

| # | 唤醒词 | 功能 | 动作 | 说明 |
|---|--------|------|------|------|
| 1 | 识别图片 | 图片内容识别 | mmx vision describe | 返回图片描述 |
| 2 | 解析图片 | 深度解析（OCR、细节） | mmx vision describe --prompt 细节 | 强调细节提取 |
| 3 | 描述图片 | 自然语言描述图片 | mmx vision describe | 整体描述 |
| 4 | 提取文字 | OCR 提取图片中的文字 | mmx vision describe --prompt OCR | 专注文字/数字 |
| 5 | 判断路由 | 分析图片属于哪个技能领域 | mmx vision → 判断 | 只判断不执行 |
| 6 | 路由记账 | 识别后直接路由到饼干记账 | mmx vision → 饼干记账 | 金额/交易类图片 |
| 7 | 路由管家 | 识别后直接路由到居家管家 | mmx vision → 居家管家 | 物品类图片 |
| 8 | 路由卡路里 | 识别后直接路由到卡路里 | mmx vision → 卡路里 | 食物类图片 |
| 9 | 自动路由 | AI 自动判断并推荐技能 | mmx vision → 判断 → 推荐 | 需用户确认 |
| 10 | 批量路由 | 多张图片分别识别路由 | 循环 mmx vision | 连续发图场景 |
| 11 | 组合执行 | 一张图触发多技能 | mmx vision → 多技能 | 如食物→记账+卡路里 |
| 12 | 查看路由 | 查看最近路由结果 | 读取上下文 | 查询类 |
| 13 | 重新路由 | 对同一图片重新判断路由 | mmx vision → 判断 | 纠错场景 |

### 匹配规则

1. **图片事件优先**：用户发送图片时自动触发，无需文字唤醒词
2. **文字指令匹配**：用户输入包含表中唤醒词即命中
3. **最长匹配**：同时命中多个时，取最长的
4. **组合优先**：用户同时发图片+唤醒词时，以唤醒词为准决定路由方向

## 触发条件

两种触发方式：

1. **图片事件触发**：用户发送图片时自动触发
2. **文字指令触发**：用户输入包含路由表中任一唤醒词时触发

## 使用流程

### 流程原则

AI 根据图片内容自主判断，灵活处理。不要机械套用步骤，观察图片后自行决定：

1. **先subagent解析图片**：收到图片立即fork给subagent调用`mmx vision describe`识别内容，主agent调用`sessions_yield()`等待，不阻塞
2. **再判断是否相关**：subagent解析完成后，基于识别结果判断是否可能与（记账/居家管家/卡路里）相关
3. **最后确认执行**：展示识别结果和推荐操作，用户确认后再执行

### 判断依据

- 涉及金额/交易 → 饼干记账
- 涉及物品存放 → 居家管家
- 涉及食物/营养 → 卡路里
- 多信号叠加 → 可能需要多技能联动

### 重要原则

- **优先相信用户的直接意图**，避免过度关联
- 如果用户只是分享图片，不要主动推荐技能
- 用户明确要求记录时才调用技能

### ⚠️ 图片识别工具优先级

强制约束的前提条件：
1. **先subagent解析图片**：收到图片立即fork给subagent调用`mmx vision describe`识别内容，主agent调用`sessions_yield()`等待，不阻塞

**识别图片内容时，优先使用 `mmx vision describe`（交给subagent处理）：**
```bash
mmx vision describe --image <图片路径> --prompt "描述这张图片的内容"
```

**原因**：`mmx vision describe` 是用户配置的 MiniMax 图像识别 CLI，效果稳定。`image` 工具（调用视觉模型）经常因参数问题报错。

**只有当 mmx 识别效果不好时**，才考虑其他方案。

---

## AI执行指南（subagent异步模式）

图片路由的默认执行方式：主agent接收到图片后，立即fork给subagent处理，主agent通过yield等待结果。

### 标准流程

```
用户发图片 → 主agent fork subagent → sessions_yield() → 唤醒后继续主流程
```

### 工具调用顺序

```python
# 1. fork子agent处理图片（立即下发，不判断耗时）
subagent = sessions_spawn(
    context="fork",                                    # 继承主agent上下文
    task=f"mmx vision describe --image <{image_path}> --prompt "描述这张图片内容"",
    taskName="img_parse",                              # 任务标识
    delivery={"mode": "announce", "channel": "current"}  # 完成推送到当前session
)

# 2. 释放主agent（此时可处理其他消息/任务）
sessions_yield()

# 3. 唤醒后，subagent输出已在上下文中，直接使用
```

### 关键约束

- 主agent**不判断**耗时，图片路由默认走subagent模式
- **一对一到**：每个subagent对应一次sessions_yield()
- **失败容错**：唤醒后检查exitCode，失败则降级为同步识别
- **结果获取**：唤醒后直接读取子agent输出，无需额外API调用

### 降级规则

当subagent失败时，自动降级为同步执行：
```bash
mmx vision describe --image <图片路径> --prompt "描述这张图片内容"
```

---
### 用户确认

无论识别结果如何，都需要展示给用户并确认：
- 识别出了什么
- 建议执行哪些操作
- 用户选择后执行

每个技能执行完成后询问是否继续，不需要一次确认所有要执行的技能。

---