## Agent 技能

### 问题追踪

本地 Markdown — 问题以文件形式存放在 `.scratch/<功能名>/` 下。详见 `docs/agents/issue-tracker.md`。

### Triage 标签

保留默认值：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### 领域文档

单一上下文 — 在 `备忘录/` 根目录放一份 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。
