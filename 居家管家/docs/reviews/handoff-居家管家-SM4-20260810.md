# Handoff · 居家管家 SM4 统计总览域验收 · 2026-08-10

> 由 Mavis 在 #118 关闭时生成。新 session 接手居家管家 SM4 相关工作以此为准。

## 当前进度快照

- **#118 已关闭**（T5 审查 · SM4 统计总览域 4 场景双浏览器验收 + 人工逐场景验收）
- **测试基线**: 390 passed（居家管家 tests/，DB 隔离见下）
- **本次验收 8 项修复全部入库并 push**

## 本次验收修复清单（全部已 push 远端）

| commit | 修复 |
|--------|------|
| 3394fd0 | 分类分布子类归并(数码消失/食物1实为3) + 桌面操作栏遮挡 + expiring 颜色语义 |
| 959a1fc | stats CLI 自动中文命名(`<场景名>_结果_<TS>.html`,卡路里同款) + 4 模板按钮触控 ≥40/44px |
| 0b9470f | 折线图数据点正圆(SVG circle 被 preserveAspectRatio=none 拉成椭圆 → HTML 绝对定位圆点) |
| cda1cd0 | prompt 填写格式统一(字段名(提示): ______) · SM4 域(scenarios.yaml + 模板 + py 源头) |
| 0b6e0a6 | 复制按钮契约 v2: 复制数据可见化(非层级JSON) + 点击Toast + 按钮文字/颜色恒定 + 镜像同步 |
| e4a7363 | 闲置/过期确认处理回执自包含化(技能加载+唤醒词+逐物品动作) |
| b499333 | 确认处理回执去脚本化(人类语言命令式,禁写底层 CLI 防耦合) |
| 82756f3 | 回执措辞修正: 结果导向→动作导向(「帮我处理以下物品」,确保 AI 执行而非汇报) |

## 剩余工作（SM4 相关已清零）

- SM4 统计总览域 4 场景验收全部通过，无遗留
- map #104「居家管家实施编排 map」仍有其他域进行中（并发 session）

## 验收流程规范（用户拍板 · 跨场景强制）

1. **每场景交付 = 场景 HTML + HELP「复制 prompt」内容一起看**（prompt 来源 references/scenarios.yaml）
2. **5 维对抗式审查**: ①复制数据/日志按钮(文字恒定+Toast) ②呈现数据 vs 契约 ③375px 手机适配 ④顶级设计师风格(Apple 风) ⑤体验增强建议(主动提, 用户拍板)
3. **按钮颜色 ≤3 色 + 按功能区分**（同色系算 1 色, 白色不计）
4. **文件命名**: `<场景名>_结果_<TS>.html`（卡路里同款规则, stats CLI 已内置自动命名）
5. **回执 prompt 原则**: 命令式 + 动作导向 + 不写底层 CLI 脚本（AI 加载技能后自行映射）
6. **发 media 前逐一 Test-Path**

## 关键工程规则

- **DB 隔离红线**: 任何自测/验证用 `SKILLS_DB_PATH=居家管家/.scratch/t5-review-118/db`（种子 9 物品）或 fixture 临时库；生产 DB 禁止触碰
- **commit**: 全中文 + `Tested-By` 行; `git commit -- <paths>` 限定路径（并行会话共享 index）
- **push**: 与用户确认后 push（本 session 已 push 全量）
- **并行会话活跃**: 卡路里/私家大厨/作息管家/饼干记账，commit 可能被抢锁
- 复杂 Python 逻辑一律写脚本文件（PowerShell 引号地狱）
- 视觉验收: 小图形/精度必须 DOM 测量 + 像素分析兜底（视觉模型不可靠）

## 本轮关键产出

- 验收报告: `居家管家/docs/reviews/118-SM4统计总览域验收报告.html`（已入库）
- 结论表: issue #118 评论（含 6 项补充修复表）+ 已 close
- map 更新: issue #104 Decisions so far 追加（人工验收闭环）
- 审查产物快照: `居家管家/.scratch/t5-review-118/`（截图 + review_report.json + 验收 HTML ×4 + HELP，.scratch 不入库）

## 记忆/共识沉淀位置

- user memory: HTML 验收 5 维 / 每场景附 prompt / 复制按钮契约 v2 / 按钮颜色规则 / 回执 prompt 措辞原则(命令式+动作导向+不写脚本) / 交付完整性审计
- agent memory: 视觉验证纪律（视觉模型对小图形/精度不可靠 → DOM/像素测量兜底）
- 居家管家 AGENTS.md: 场景路由/CLI 约定
