# handoff · 居家管家 SM5 快递购物域(2026-08-10)

> 本 handoff 供下一 session / 其他 agent 接手时快速对齐 SM5 域现状。

## 状态

- **实施**: ✅ 完成(ISSUE #110,commit e6ef7b2 起)
- **自动审查**: ✅ G6 5 阶段 PASS(审查 ISSUE #121)
- **人工验收**: ✅ **4/4 场景全部通过**(2026-08-10 用户逐场景验收,MMX 视觉 9.5-10 分)

## 4 场景清单

| 场景 | 唤醒词 | 模板 | CLI |
|---|---|---|---|
| 购物清单 SM5-1 | 购物清单 | templates/快递购物/list.html | cli.py list / list-add / list-check |
| 缺货检测 SM5-2 | 缺货检测 | templates/快递购物/missing.html | cli.py missing / missing-to-list |
| 快递跟踪 SM5-3 | 查快递 | templates/快递购物/express.html | cli.py express / express-receive |
| 囤货盘点 SM5-4 | 囤货盘点 | templates/快递购物/stock.html | cli.py stock / stock-set-threshold / stock-fix |

## 数据层

- 新增 2 表: `shopping_items`(购物清单条目)、`stock_thresholds`(囤货阈值)——实施期域内自持幂等建表,物理收编归 **D1 批**总账 #103
- 快递跟踪**不建表**: 复用 `item_locations.location_status='快递中'`(位置记录 created_at 推算等待天数)
- 记录契约: 收货/修正写 `item_events`(EVENT_STATUS_CHANGED / EVENT_QUANTITY_CHANGED)

## 已落地规范(2026-08-10 验收拍板)

1. **按钮布局**: 偶数 2 列排满;奇数重要按钮单独一行;复制数据/复制日志单独一行
2. **按钮颜色 ≤3**: 蓝=操作(实心主/线框次) · 灰=工具(复制) · 红=危险(清记录)
3. **复制 prompt 格式** (issue #248): `请加载「居家管家」技能,帮我<动作>(唤醒词:<词>):` + `参数名(推荐值): ___` 每行一个
4. **复制数据/日志人类可读**: 【场景名 · 数据快照】/ ①-⑥ 分段(非 JSON)
5. **中文文件名**: `场景名_YYYYMMDD_HHMMSS.html`(render/__init__.py TEMPLATE_TO_COMMAND_CN 已注册 SM5 映射)

## 验收发现并已修的关键 bug

- 复制数据按钮引用 render 局部变量 → 挂 `window.__hmPayload`
- onclick 属性内 `\n` 需 `\\n` 转义(模板字符串会解释成真实换行)
- `.badge.empty` 撞 `.empty` 空态类 → 改 `b-empty`/`b-full`

## 遗留观察项(转公共层 ISSUE 候选)

1. error.html 复制日志 ④ 调用链为空(call_chain="")——08 §6.1,公共层统一补齐
2. missing_to_list note 显示「当前 -」——合理降级,非 bug
3. 商品来源标签上下 padding +1-2px(精益求精级)

## Git

- 提交: `235a9e9`(4 场景验收修复)+ `4ebef71`(囤货/快递收尾)
- 均 pytest 390 全绿;**未 push**(用户统一 push)
- 测试隔离: 全程 `SKILLS_DB_PATH` 指向 `.scratch/t6-review/db`,未触碰生产库

## 下一步(与本域相关)

- SM5.yaml status 翻转(SKILL.md 路由已注册,待合并器接入后翻转)
- D1 批收编 shopping_items/stock_thresholds 物理建表(总账 #103)
- 公共层: error 回执 call_chain 补齐
