从第一性原理出发，对抗式审查：
1、复制数据、复制日志等按钮是否符合要求“1. 交互反馈：
(a) 点击后按钮的内容不会改变，且不能换行。
(b) 点击每一个按钮下面都会有 toast 提示，特别是那些复制 prompt 的按钮，必须有这种 toast 提示。
2. 动画效果：
(a) 椭圆形的按钮在点击时，不要出现背景长方形框的点击动画，这种长方形的感觉非常糟糕。
(b) 建议点击后不要有任何动画效果，下面直接弹出 toast，我觉得是最好的。”
2、整个HTML呈现的数据是否符合设计时的场景要求和呈现数据的要求
3、HTML中各个组件和内容有没有做好手机版适配，包括但不限于：图表
4、整个HTML的前端样式是否符合 顶级设计师的设计风格
5、有没有你认为可以让用户使用起来更舒服更有体验更能看到有价值数据的方案技巧
6、你需要读取 HTML 页面中的每一行代码，不能用脚本对它进行审查。你需要从代码层面以及截图后的 MMX 分析层面，多维度地去判断：

1. 当前的 UI 符不符合一个高级 UI 设计师的水平？
2. 如果你是用户，在手机上使用这个 HTML 页面，体验怎么样？
3. 它的数据呈现、功能展示、设计风格等方面，能不能都达到 90 分以上（满分 100 分）？

如果不能做到，你就需要进行优化；如果可以做到，我们就可以认可。最终你把这个 HTML 页面优化好之后，让我来看。

## toast格式要求和样式要求

## prompt格式要求

对于需要用户填写信息的 Prompt，其格式要求如下：

如果其中有部分需要用户填写某些数据，其形式应为：
参数名字（推荐值）：___

具体说明：
1. 推荐值：括号内推荐的值，必须是我们功能实际支持的。
2. 填写区域：冒号后使用下划线，让用户去填写。
3. 排版要求：每一个参数占 1 行。

https://github.com/FeatherHunter/SKILLS/issues/248

## 复制数据和复制日志

这两个按钮复制出来的信息不能默认是 JSON 格式，需要是那种人类可以看懂的格式。

这两个按钮单独一行。

## 按钮要求

1. 在手机端当前页面的按钮布局与颜色要求如下：

1. 布局规则：
(a) 数量为偶数时：一行放两个按钮，有多少按钮就放多少行。
(b) 数量为单数时：一行放两个按钮，某些比较重要的按钮单独占据一行。

2. 颜色要求：
(a) 按钮颜色需按照各自对应的功能进行区分。
(b) 整个页面所有按钮的颜色不能超过 3 个，不能五颜六色、五彩缤纷。