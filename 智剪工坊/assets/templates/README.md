# templates/

模板文件(预制转场预设、文字模板、剪辑模板)。

## 当前状态

🚧 **空目录** —— 后续会加:

- `transition_presets.json` —— 常用转场组合(比如"科普视频用 fadeblack", "vlog 用 fade")
- `subtitle_styles.json` —— 字幕样式预设(字体/颜色/位置)
- `cover_layouts.json` —— 封面布局模板
- `pipeline_templates/` —— 各场景的流水线模板(健身/旅行/美食/教程)