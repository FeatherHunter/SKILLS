# 私家大厨 🍳

> 版本：v3.0
> 设计：基于17张表，6个核心用例，无删除操作
> 路由：意图关键词 + 菜名提取 + 优先级 + 兜底search

---

## ⚠️ 操作规范（强制）

本技能所有数据操作必须通过 CLI，禁止直连数据库。

---

## AI使用规范（强制）

### 调用任何manager前，必须：

1. **展示完整参数示例** — 让用户知道这条命令会用哪些参数
2. **对未提供字段进行合理推测** — 基于菜系特点/常见值/烹饪逻辑
3. **推测不出时询问用户** — 不能留空不填

### 字段推测规则（仅录入时使用）

> 详见 `features/add.md` — 仅在录入食谱时调用，查看/做菜模式不需要此规则。

---

## 路由算法（核心）

### 匹配流程

```
用户输入
    ↓
第1步：意图识别
    按优先级从高到低，扫描触发关键词
    命中即确定意图，不再往下匹配
    ↓
第2步：菜名提取
    触发词后面的内容 → 去掉空格 → 剩余文字
    菜名必须 ≥2 个字符，否则视为无效菜名
    ↓
第3步：功能分流
    意图 + 菜名 → 加载对应 features/*.md
    菜名缺失 → 追问用户（不能猜）
    ↓
第4步：兜底
    没有任何触发词命中 → 默认走 search.md（可能是纯菜名输入）
```

### 匹配规则

| # | 规则 | 说明 |
|---|------|------|
| R1 | 优先级排序 | P1>P2>P3>P4>P5>P6>P7，高优先级命中直接分流 |
| R2 | 最长匹配 | 同优先级下，选触发词最长的 |
| R3 | 关键词独立 | 触发词是独立词，不是子串匹配 |
| R4 | 菜名"后面抓" | 触发词后面紧跟的文字视为菜名 |
| R5 | 兜底 search | 没有任何关键词命中 → 走 search.md |
| R6 | 追问确认 | 菜名提取失败（<2字符或为空）→ 追问用户 |

---

## 路由表（意图 + 触发词）

### P1：做菜模式

**触发关键词**（任一命中即触发）：
| 触发词 | 说明 |
|--------|------|
| `做菜模式` | 完整词 |
| `开始做` | 后面跟菜名，如"开始做红酒慢炖牛腩" |
| `进入烹饪` | 完整词 |
| `开始烹饪` | 完整词 |
| `做这道菜` | 完整词 |

**分流**：`view.md` → **功能二：做菜模式**

---

### P2：查看食谱

**触发关键词**（任一命中即触发）：
| 触发词 | 说明 |
|--------|------|
| `看看` | 后面跟菜名，如"看看宫保虾球怎么做" |
| `菜谱` | 后面跟菜名，如"宫保虾球菜谱" |
| `做法` | 后面跟菜名，如"宫保虾球做法" |
| `步骤` | 后面跟菜名，如"宫保虾球步骤" |
| `详情` | 后面跟菜名，如"红酒炖牛肉详情" |
| `查看` | 后面跟菜名，如"查看宫保虾球" |
| `给我看` | 后面跟菜名，如"给我看看宫保虾球" |

**分流**：`view.md` → **功能一：查看食谱**（先展示食谱，末尾问"要开始做吗？"）

---

### P3：搜索筛选

**触发关键词**（任一命中即触发）：
| 触发词 | 说明 |
|--------|------|
| `找` | 后面跟搜索词，如"找川菜" |
| `搜` | 后面跟搜索词，如"搜一下排骨" |
| `有哪些` | 后面跟类型，如"川菜有哪些" |
| `哪些菜` | 后面跟条件，如"哪些菜里有虾" |
| `什么菜` | 后面跟条件，如"什么菜用到虾" |
| `哪个` | 后面跟条件，如"哪个菜简单" |

**分流**：`search.md`

---

### P4：修改食谱

**触发关键词**（任一命中即触发）：
| 触发词 | 说明 |
|--------|------|
| `改成` | 后面跟修改内容，如"把宫保虾球第2步改成小火" |
| `换成` | 后面跟修改内容，如"换成红烧做法" |
| `加食材` | 后面跟食材，如"给宫保虾球加食材" |
| `加个食材` | 后面跟食材，如"加个食材" |
| `修改` | 需要上下文补充菜名 |
| `难度` | 需要上下文补充菜名，如"宫保虾球难度太高了" |

**分流**：`update.md`

**注意**：孤立的"改一下"不作为触发词，用户必须指明要改什么。

---

### P5：烹饪历史

**触发关键词**（任一命中即触发）：
| 触发词 | 说明 |
|--------|------|
| `做过` | 后面跟菜名，如"宫保虾球做过几次了" |
| `评分` | 后面跟菜名，如"给宫保虾球评分" |
| `历史` | 后面跟菜名，如"宫保虾球历史" |
| `复盘` | 后面跟菜名，如"复盘一下宫保虾球" |
| `记录` | 需要上下文补充菜名，如"记录今天做的菜" |
| `烹饪记录` | 需要上下文补充菜名 |

**分流**：`history.md`

---

### P6：采购清单

**触发关键词**（任一命中即触发）：
| 触发词 | 说明 |
|--------|------|
| `采购` | 后面跟菜名，如"宫保虾球采购清单" |
| `清单` | 后面跟菜名，如"宫保虾球清单" |
| `买` | 后面跟条件，如"要买什么" |
| `要买` | 后面跟菜名，如"要买宫保虾球的材料" |
| `准备食材` | 需要上下文补充菜名 |

**分流**：`shopping.md`

---

### P7：录入食谱

**触发关键词**（任一命中即触发）：
| 触发词 | 说明 |
|--------|------|
| `录入` | 后面跟菜名，如"录入一个新菜" |
| `新建` | 后面跟菜名，如"新建一个食谱" |
| `添加` | 后面跟菜名，如"添加一道菜" |
| `收藏` | 后面跟菜名，如"我要收藏一个新菜" |

**分流**：`add.md`

---

## 菜名提取示例

| 用户输入 | 触发词 | 提取菜名 | 路由 |
|---------|--------|---------|------|
| "开始做红酒慢炖牛腩" | `开始做` | 红酒慢炖牛腩 | view.md (做菜模式) |
| "看看宫保虾球怎么做" | `看看` | 宫保虾球 | view.md (查看模式) |
| "宫保虾球菜谱" | `菜谱` | 宫保虾球 | view.md (查看模式) |
| "找川菜" | `找` | 川菜 | search.md |
| "哪些菜里有虾" | `哪些菜` | 无（菜名<2字符） | search.md（筛选"虾"） |
| "搜一下排骨" | `搜` | 排骨 | search.md |
| "宫保虾球做过几次了" | `做过` | 宫保虾球 | history.md |
| "生成采购清单" | 无命中 | 无 | shopping.md（追问菜名） |
| "宫保虾球" | 无命中 | 宫保虾球 | search.md（兜底菜名搜索） |

---

## 快速导航

| 功能 | 参考文档 | 优先级 |
|------|----------|--------|
| 录入食谱 | `features/add.md` | P7 |
| 查看食谱 + 做菜模式 | `features/view.md` | P1/P2 |
| 搜索筛选 | `features/search.md` | P3 |
| 修改食谱 | `features/update.md` | P4 |
| 烹饪历史 | `features/history.md` | P5 |
| 采购清单 | `features/shopping.md` | P6 |
| 数据库结构 | `references/database_schema.md` | - |
| 分类参考 | `references/categories.md` | - |
| CLI命令 | `references/commands.md` | - |

---

## 核心原则

### 只增不删
- 没有物理删除操作
- 整道食谱废弃用 discard（status = '已废弃'），自动从列表/搜索中过滤
- 已废弃食谱可通过 show 查看简要信息

### 交互确认
- 所有写操作前必须展示，用户确认后执行
- 多选项时让用户选择，不默认

### 自然语言解析
- AI 负责解析用户意图，转换为 CLI 命令
- 信息不完整时追问，不猜测

---

## 表结构

共17张表，详见 `references/database_schema.md`

---

## 与其他技能联动

| 技能 | 联动场景 |
|------|---------|
| 卡路里 | 采购食品时可同步记录营养成分 |
| 居家管家 | 炊具借用/归位时参考 |

**处理原则**：主动思考是否需要联动，先完成主技能，再询问用户。